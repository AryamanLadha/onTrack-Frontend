//how to use
//font: ${({ theme }) => theme.font.display1};
// color: ${({ theme }) => theme.color.primary};

const color = {
    // colors
  };
  
  /**
   * bold: 700
   * medium: 600
   * regular: 400
   * light: 300
   */
  const font = {
    // fonts 
  };
  
  const theme = {
    color,
    font,
  };
  
  export default theme;